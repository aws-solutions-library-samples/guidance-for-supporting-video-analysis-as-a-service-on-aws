package com.amazonaws.videoanalytics.videologistics.schema;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.Getter;
import lombok.NoArgsConstructor;
import software.amazon.awssdk.enhanced.dynamodb.mapper.annotations.DynamoDbBean;
import software.amazon.awssdk.enhanced.dynamodb.mapper.annotations.DynamoDbPartitionKey;
import software.amazon.awssdk.enhanced.dynamodb.mapper.annotations.DynamoDbAttribute;
import software.amazon.awssdk.enhanced.dynamodb.mapper.annotations.DynamoDbUpdateBehavior;
import software.amazon.awssdk.enhanced.dynamodb.mapper.UpdateBehavior;
import software.amazon.awssdk.enhanced.dynamodb.extensions.annotations.DynamoDbAutoGeneratedTimestampAttribute;
import software.amazon.awssdk.enhanced.dynamodb.mapper.annotations.DynamoDbSecondaryPartitionKey;
import com.amazonaws.videoanalytics.videologistics.schema.SchemaConst;

import java.time.Instant;

@DynamoDbBean
@Builder
@AllArgsConstructor
@NoArgsConstructor
@Data
public class VLRegisterDeviceJob {
    @Getter(onMethod_ = { @DynamoDbPartitionKey, @DynamoDbAttribute(SchemaConst.JOB_ID) })
    private String jobId;

    @Getter(onMethod_ = { @DynamoDbAttribute(SchemaConst.KVS_STREAM) })
    private String kvsStreamArn;

    @Getter(onMethod_ = { @DynamoDbAttribute(SchemaConst.JOB_STATUS) })
    private String status;

    @Getter(onMethod_ = { @DynamoDbAttribute(SchemaConst.DEVICE_ID) })
    private String deviceId;

    @Getter(onMethod_ = { @DynamoDbAttribute(SchemaConst.PLAYBACK_SIGNALING_CHANNEL_ARN) })
    private String playbackSignalingChannelArn;

    @Getter(onMethod_ = { @DynamoDbAttribute(SchemaConst.CREATED_AT) })
    private String createTime;

    @Getter(onMethod_ = { @DynamoDbAttribute(SchemaConst.LAST_UPDATED) })
    private String lastUpdated;

    @Getter(onMethod_ = { @DynamoDbAttribute(SchemaConst.ERROR_MESSAGE) })
    private String errorMessage;

    @Getter(onMethod_ = { @DynamoDbAttribute(SchemaConst.WORKFLOW_NAME) })
    private String workflowName;

    @Getter(onMethod_ = { @DynamoDbAttribute(SchemaConst.LIVE_STREAM_SIGNALING_CHANNEL_ARN) })
    private String liveStreamSignalingChannelArn;
}