import { BootstrapStack } from "../lib/stacks/bootstrapStack/bootstrapStack";
import { createStackSnapshotTest } from "video_analytics_common_construct";

createStackSnapshotTest("BootstrapStack", BootstrapStack);
